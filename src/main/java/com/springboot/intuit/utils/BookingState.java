package com.springboot.intuit.utils;

public enum BookingState {
    REQUEST_EXCHANGE,
    BORROWED,
    RETURN_REQUESTED,
    REQUEST_BOOKING,
    RETURN_ACCEPTED,
    COMPLETED,
    PERMANENT_EXCHANGE,
    CANCELLED
}